# Virtual environment
venv/

# Secrets and credentials
.env
.env.bak
private.json

# Python bytecode
__pycache__/
*.pyc

# OS
Thumbs.db
.DS_Store

# SQLite database files (regenerable from API)
*.db
*.db-journal
*.db-wal
*.db-shm

# Manager real-name -> alias map. Do not commit — the whole point is to
# keep real last names out of the public repo.
manager_aliases.json

# Excel workbooks - Pete's manual tracking, kept in OneDrive/source-of-truth
*.xlsx
~$*.xlsx

# Analysis intermediates - regenerable, not source
diff_*.csv
drc_validation*.csv
trades_db_export.csv
teams_export.csv
db_only_review.txt

# Setup scratch
new_pc_setup_checklist.md
scan_my_pc.ps1
setup_summary.md
system_report.txt
test_write.txt
_write_test.txt

# ADP review output (regenerable)
adp_unmatched.csv

# Private league correspondence — source materials only, public renderings live in comms/
League Comms and Context/
External Sources/IMG_*

# PowerPoint lock files (same idea as the ~$*.xlsx rule above)
~$*.pptx

# Control-panel run log — local only, regenerated on every run
runs.json

# Deck QA preview renders — regenerable scratch
qa_dash/

# Backups - manual snapshots; git history is the real backup
Backups/

# Working drafts and private prep - final versions publish via comms/
Roster Preview - Fact Pack (2026).md
rules_reconciliation_ledger_DRAFT.html
yahoo_api_application_draft.md

# Preview renders - regenerable scratch
lottery_section_preview.html

# Design Brief bulk items - dashboard copy is in git history, zip is a transfer bundle
Design Brief/Current Dashboard (reference - do not edit).html
Design Brief/GitHub dashboard review setup.zip
